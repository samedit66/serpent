deferred class
    HASHABLE
-- Интерфейс для объектов, которые могут быть использованы в качестве ключей в хэш-таблице.

feature

    hash_code: INTEGER
    -- Возвращает хэш-код объекта.
    deferred
    end

end
