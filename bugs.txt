1) Не работает наследование с дженериками
2) Не стоит пытаться вызывать конструктор по умолчанию у extended типов
3) Не вставляется автоматическое преобразование типов между INTEGER и REAL
4) В двойных кавычках строк нельзя записывать одинарные, в лексере они подвергаются экранированию,
а спецификация JSON не поддерживает экранирование одинарных кавычек
